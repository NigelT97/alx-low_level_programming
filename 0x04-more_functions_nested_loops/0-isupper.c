#include "main.h"
/**
 * _isupper - check for upper case
 * @c: variable for upper case
 * Return: 1 if upeer case amd 0 if not
 */
int _supper(int c)
{
	if (c >= 65 && c <= 90)
		return (1);
	else
		return (0);
}
